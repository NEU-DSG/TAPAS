<%= @user.name %> (<%= @user.email %>) has been vetted by an admin and accepted your invitation to join "<%= @project.title %>".

To confirm their membership and activate their access, visit:

<%= @confirm_url %>
